<h1>Books!</h1>

<h2>Books that I have not read!</h2>

<ul>
  {{#each books}}
    {{#unless hasRead}}
      {{> books/book-block hasRead=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Books I've Read!</h2>

<ul>
  {{#each books}}
    {{#if hasRead}}
      {{> books/book-block hasRead=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Book</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Book Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="has-read">Read it?</label><br>
    <input type="radio" name="has-read" value="1" checked> Read it!<br>
    <input type="radio" name="has-read" value="0"> Need to read
  </div>

  <button type="submit">Add Book</button>
</form>
